import React, { Component } from 'react';  //eslint-disable-line
import { history } from '../../redux/store';

class Navigation extends Component {
  /**
   * Render Navigation
   * @return {JSX} Rendered Navigation
   */

  render() {
    return (
      <nav className='nav'>
        Left navigation
      </nav>
    );
  }
}
export default Navigation;
